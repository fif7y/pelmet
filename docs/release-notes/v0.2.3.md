# Pelmet 0.2.3

Fixes:

- Reordering icons by ⌘-dragging them in the menu bar now syncs into the
  layout editor even when "Show Pelmet icon in the menu bar" is turned off.
  Previously the editor kept the old order until you reordered there too.
